# 🔧 คู่มือแก้ไข Error: origin_mismatch

## ❌ ปัญหา: Error 400: origin_mismatch

เมื่อพบ error นี้หมายความว่า **URL ที่ใช้เปิดไฟล์ HTML ไม่ตรงกับ Authorized JavaScript origins** ที่ตั้งค่าไว้ใน Firebase Console

---

## ✅ วิธีแก้ไข

### วิธีที่ 1: ผ่าน Firebase Console (แนะนำ)

1. **ไปที่ Firebase Console**
   - https://console.firebase.google.com
   - เลือก Project ของคุณ

2. **ไปที่ Authentication Settings**
   - คลิก **Authentication** ในเมนูด้านซ้าย
   - คลิกแท็บ **Settings**
   - เลื่อนลงไปที่ส่วน **Authorized domains**

3. **เพิ่ม Domain**
   - คลิก **Add domain**
   - เพิ่ม domain ตามที่คุณใช้:
     - `localhost` (สำหรับเปิดไฟล์โดยตรง)
     - `127.0.0.1` (สำหรับ IP address)
     - หรือ domain อื่นๆ ที่ใช้

4. **บันทึก**

---

### วิธีที่ 2: ผ่าน Google Cloud Console

1. **ไปที่ Google Cloud Console**
   - https://console.cloud.google.com
   - เลือก Project เดียวกับ Firebase

2. **ไปที่ APIs & Services > Credentials**
   - คลิก **APIs & Services** ในเมนูด้านซ้าย
   - คลิก **Credentials**

3. **แก้ไข OAuth 2.0 Client ID**
   - หา OAuth 2.0 Client ID ที่ตรงกับ Web Client ID ของคุณ
   - คลิก **Edit** (ไอคอนดินสอ)

4. **เพิ่ม Authorized JavaScript origins**
   - ในส่วน **Authorized JavaScript origins**
   - คลิก **+ ADD URI**
   - เพิ่ม origin ตามที่คุณใช้:
     ```
     http://localhost
     http://localhost:5500
     http://127.0.0.1:5500
     ```
   - **หมายเหตุ:** ต้องใส่ protocol (`http://` หรือ `https://`) ด้วย

5. **บันทึก**
   - คลิก **SAVE**

---

## 🔍 วิธีหา Current Origin

เปิดไฟล์ HTML ใน browser แล้วดูที่:
- **Address Bar:** URL ที่แสดงอยู่
- **Console (F12):** ดู log ว่ามี origin อะไร

ตัวอย่าง:
- `http://localhost:5500` → เพิ่ม `http://localhost:5500`
- `http://127.0.0.1:5500` → เพิ่ม `http://127.0.0.1:5500`
- `file:///C:/path/to/file.html` → **ไม่รองรับ!** ต้องใช้ localhost

---

## 📝 ตัวอย่าง Authorized JavaScript origins

```
http://localhost
http://localhost:5500
http://127.0.0.1
http://127.0.0.1:5500
https://yourdomain.com
```

**หมายเหตุ:**
- ต้องใส่ protocol (`http://` หรือ `https://`) เสมอ
- ไม่ต้องใส่ path (`/` หรือ `/path/to/file.html`)
- ไม่ต้องใส่ trailing slash (`/`)

---

## ⚠️ ข้อควรระวัง

1. **File Protocol ไม่รองรับ**
   - ❌ `file:///C:/path/to/file.html` → ไม่ทำงาน
   - ✅ ต้องใช้ `http://localhost` หรือ `http://127.0.0.1`

2. **Port ต้องตรงกัน**
   - ถ้าใช้ `http://localhost:5500` ต้องเพิ่ม `http://localhost:5500` ใน Authorized origins
   - ไม่ใช่แค่ `http://localhost`

3. **HTTPS vs HTTP**
   - ถ้าใช้ `https://` ต้องเพิ่ม `https://` ใน Authorized origins
   - ถ้าใช้ `http://` ต้องเพิ่ม `http://` ใน Authorized origins

---

## 🚀 วิธีทดสอบ

1. **เพิ่ม Authorized origin** ตามวิธีด้านบน
2. **รอสักครู่** (อาจใช้เวลา 1-2 นาทีในการอัพเดท)
3. **Refresh หน้าเว็บ** (กด F5)
4. **ลอง Sign-In อีกครั้ง**

---

## 💡 Tips

- **ใช้ VS Code Live Server:** จะใช้ `http://127.0.0.1:5500` หรือ `http://localhost:5500`
- **ใช้ Python Simple Server:** จะใช้ `http://localhost:8000`
- **ใช้ Node.js http-server:** จะใช้ `http://localhost:8080`

ตรวจสอบ port ที่ใช้แล้วเพิ่ม origin นั้นใน Firebase Console

---

## 📞 ยังแก้ไม่ได้?

1. ตรวจสอบว่า Client ID ถูกต้อง
2. ตรวจสอบว่า origin ที่เพิ่มถูกต้อง (มี protocol, ไม่มี trailing slash)
3. รอสักครู่แล้ว refresh หน้าเว็บ
4. ลอง clear cache ของ browser
5. ตรวจสอบ Console (F12) เพื่อดู error message เพิ่มเติม


